import React from 'react';
import ReactDOM from 'react-dom/client';
import './styles/site.scss'; // Import global styles

// Future: Widget auto-mount infrastructure
// This will be used to automatically mount React widgets on Razor pages
// Example:
// document.querySelectorAll('[data-react-widget]').forEach((element) => {
//   const widgetName = element.getAttribute('data-react-widget');
//   const props = JSON.parse(element.getAttribute('data-props') || '{}');
//   // Mount appropriate widget based on widgetName
// });

console.log('TechCorp Content Hub - Razor + React Widgets Ready');
